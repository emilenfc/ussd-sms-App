import { Credentials } from '../../../utils/getFullCredentials.types';
import { SendChatTextMessage } from './sendChatTextMessage.types';
export declare const sendChatTextMessage: (credentials: Credentials) => SendChatTextMessage;
