import { SendSms, SendPremiumSms } from './sendMessage.types';
import { Credentials } from '../../utils/getFullCredentials.types';
export declare const sendSms: (credentials: Credentials) => SendSms;
export declare const sendPremiumSms: (credentials: Credentials) => SendPremiumSms;
